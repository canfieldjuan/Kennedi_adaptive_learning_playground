# v0.3.45 Bear Art Studio slice 2 — story cards, dress-up, gallery finish

Second slice of the Art game: the spec's remaining progression level (Story
Card Art), the dress-up surface the child loves, and the gallery finish
moment. Presentation + content on the existing runtime; no engine changes.

## Before Code

### Root Cause (slice)

Slice 1 shipped five modes but left three spec items unbuilt: Level 6
Story Card Art (vocabulary/category reasoning — the only remaining spec
level), an outfit/dress-up art surface (top of the child's loves; slice 1
has only card/scarf surfaces), and the "obvious finish moment" upgrade
(the spec's completion beat: the art visibly becomes a kept piece — today
the card just stops).

### Required Change Surface

- `src/modules/bear-art-studio/BearArtStudioActivity.ts` —
  - new mode `story_card`: the bear speaks a one-line story; the request
    card shows HOW MANY things belong (dashed mini-slots), never which;
    the tray holds a sticker pool (story-fitting + distractors); tapping a
    fitting sticker pops it onto the card (`correct` per pick), tapping a
    distractor wiggles gently (`incorrect`, with story metadata); two
    misses glow one fitting sticker (`hint_used`, never auto-places);
    placing every story sticker completes with the selected/required sets
    in metadata. Skill: `vocabulary` (level 2 "uses words flexibly in
    pretend play", difficulty 4).
  - `free_decorate` gains an optional `surface: 'shirt'` — the card
    renders as Baby Polar Bear's shirt (inline SVG backdrop) and swatch
    taps paint the shirt itself; stickers decorate it. No other mode or
    rule changes.
  - gallery finish beat: on completion every mode's board piece gains an
    `is-gallery` class — a one-shot lift-and-frame animation
    (reduced-motion disabled), so finished art visibly becomes a kept
    piece.
- `src/modules/bear-art-studio/studio-art.ts` — `moon` sticker (story
  distractor; spec asset list) + `shirtSurfaceSvg` (paintable fill);
  render-checked.
- `bear-art-studio.types.ts` — `StoryCardContent`, surface field.
- `src/content/activities/` — `art-studio-story-outside.json` (language /
  vocabulary / different_prompt_mode, brief-linked, prompt_mode spoken:
  the story names NO sticker; the child infers what belongs) and
  `art-studio-dress-bear.json` (art / color_fill /
  same_format_new_examples, shirt surface, chain finale).
  `art-studio-fix-card.json` version 2: gains `next_activity_id` so the
  chain becomes fix → story → dress (ends Home).
- `src/content/activity-catalog.ts` — register both.
- `src/content/curriculum/curriculum.v1.json` — DATA ONLY:
  `vocabulary.activity_contexts` += story activity;
  `color_fill.activity_contexts` += dress activity. No new skills.
- `src/styles/child-ui.css` — story tray/mini-slot styles, shirt surface,
  `is-gallery` one-shot (+ reduced-motion entry), phone compaction.
- Tests — `tests/modules/bear-art-studio.test.ts`: story picks/distractor
  rejection/hint/completion evidence; shirt surface + paint; gallery
  class on completion; chain extension. `validate-activities.test.ts`:
  a story-card detector (vocabulary different_prompt_mode: the prompt
  must NOT name any required sticker; pool must hold ≥1 distractor) —
  the slice-1 detector precedent.
- `docs/captures/bear-art-studio/` — story + dress desktop, story mobile
  ~390×844, a gallery-finish frame; WebP.
- `package.json` / `package-lock.json` — `0.3.45`; `MVP_BASELINE.md` note.

### Explicit Non-Scope / Must Not Change

- Slice-1 modes' rules, events, and activities (except fix-card's chain
  pointer + version bump) — untouched.
- Engines, schema, parent rules, `ColoringActivity`, other games — 0 lines.
- No multi-round session state, no gallery persistence (the finish beat is
  visual only), no parent-recorded audio (voice lane), no new skills.
- Feedback stays gentle: distractor rejection is a soft question, never a
  failure state.

### Assumptions / Blockers

- `vocabulary` is exercised honestly: the story prompt names zero required
  stickers, so success requires word/category knowledge, not echo. The
  request card deliberately shows only the COUNT of story items.
- `different_prompt_mode` is in vocabulary's planned contexts (verified);
  the story activity's context_id is distinct from the video-vault
  response context, so it adds a genuine second context.
- Blockers: none known.

### Verification Plan

- `npm run typecheck` / `npm test` / `npm run build` / `git diff --check`;
  new art render-checked (SVG → PNG); live dev-server captures committed.

## Contract Amendments

- **Version renumbered 0.3.44 → 0.3.45.** PRs #68 (readiness proof) and #69
  (Codex's structured counting studio variant, art-studio-five-flowers)
  merged mid-slice; origin/main merged in, catalog and gap-inventory
  conflicts resolved as unions.
- **Story prompt shortened after live mobile capture.** The shared
  `.activity-prompt` clips at ~2 lines on phones (pre-existing, all games);
  the original story line was the first prompt long enough to hit it. Fixed
  by honoring the "minimal text" UX rule (73 chars), not by touching the
  shared clip. Caught by looking.
- **Shirt slot fit tightened after live capture.** The 2×2 slot grid
  overhung the shirt torso and pushed Finish art below the fold at 900px;
  card width/padding adjusted so all four slots sit on the shirt.
- **Two gap-inventory tests updated (with #69's union).** The story card
  fills the pinned `vocabulary:2:4-5` content gap in
  `curriculum-graph.test.ts` and `difficulty-coverage.test.ts` — the
  coverage engines behaving as designed; #69 simultaneously filled
  `counting:1:2-3`.

## Cold Diff Audit

### Gaps

- change without contract trace: none (the four amendments above cover
  everything beyond the original surface).
- contract requirement not delivered: none.
- protected surface touched: none — 0 lines in engines, schema, other
  games, `ColoringActivity`, or slice-1 mode logic (fix-card changed only
  by chain pointer + version 2; #69's five-flowers untouched).

### Change By Change Reconstruction

- `BearArtStudioActivity.ts` (+170) — `story_card` renderer (count-only
  request card, belongs/distractor picks with per-pick evidence, hint
  glows a missing story sticker, completion carries selected vs required
  sets); `free_decorate` shirt surface (backdrop + swatch repaints the
  shirt fill); `finishPiece` gains the gallery beat (`is-gallery` on the
  finished piece, passed by all six modes); fail-closed parsing for both.
- `studio-art.ts` (+20) — moon sticker + `shirtSurfaceSvg` (paintable
  fill); render sheet extended; both render-checked.
- `bear-art-studio.types.ts` (+20) — `StoryCardContent`, `surface` field.
- `art-studio-story-outside.json` (new) — language / vocabulary d4 /
  different_prompt_mode (brief-linked); day-garden story vs night-sky
  distractors; prompt names no sticker (detector-enforced).
- `art-studio-dress-bear.json` (new) — art / color_fill d1 / weak
  transfer; shirt surface; her palette (pink/yellow/orange/red);
  chain finale.
- `art-studio-fix-card.json` (v2) — chain pointer to the story card.
- `activity-catalog.ts` / `curriculum.v1.json` — registrations +
  data-only activity_contexts (vocabulary, color_fill).
- `child-ui.css` (+120) — request mini-slots, story slots, placed-chip
  dimming, shirt surface (+ fit amendment), gallery lift keyframes +
  frame ring, reduced-motion entry, phone compaction.
- `tests/modules/bear-art-studio.test.ts` (+100) — story accept/reject/
  hint/completion evidence, no double-place, shirt render + repaint +
  finish metadata, gallery beat, chain integrity.
- `tests/contract/validate-activities.test.ts` (+30) — story-card
  detector (spoken prompt must not name any required sticker; pool must
  hold a distractor).
- `tests/contract/curriculum-graph.test.ts` /
  `tests/core/difficulty-coverage.test.ts` — gap-inventory updates per
  the amendment (union with #69).
- `docs/captures/bear-art-studio/` (+4 WebP) — story desktop + mobile,
  dress-up, gallery finish.
- `package.json` / `package-lock.json` 0.3.45; `MVP_BASELINE.md` note.

### Contract Traceability

- story mode/dress surface/gallery beat/content/tests/captures →
  **Required Change Surface** (Root Cause: three spec items unbuilt).
- engines/schema/other games 0-line, slice-1 logic untouched, no
  persistence, no new skills, gentle feedback → **Explicit Non-Scope**.

### Gap Audit

DONE — all three slice items shipped and verified (typecheck / 419 tests /
build / diff-check / render checks / live captures). Owner look approval
requested on the four new captures.
