# v0.3.58 Kennedi's Story Stage — slice 7: visual enrichment + child-entry proposal

The arc's final slice (spec §25/slice-7): the story-room feel, the
recorded art debts, the reduced-motion pass, captures — and the
fifth-tile-versus-game-shelf child-entry PROPOSAL (§22: compare, decide,
recommend; do not implement either).

## Before Code

### Root Cause (slice)

Three art debts have been carried since slices 1/4 (the owl's face,
Biscuit's peeking pose, Luna's mane/face at scene scale); the setup
screen never got the §16 "magical story room" framing; the story-stage
surfaces are absent from the global reduced-motion block; and §22's
child-entry decision — deliberately deferred at slice 1 — still needs
its written comparison so Juan can make the call.

### Required Change Surface

- `src/modules/story-stage/story-art.ts` —
  - **Owl**: real face (brow feathers, warmer eyes) — the flagged debt.
  - **Biscuit peeking**: visible paws gripping the edge so the pose
    reads as peeking, not floating.
  - **Luna**: fuller flowing mane, ear, slightly stronger horn and
    muzzle so she reads as a unicorn at scene scale.
  - **`storyRoomValanceSvg()`** (new): a restrained curtain-valance
    strip for the setup screen — §16's story room WITHOUT the §23
    "massive decorative theater header" (short, decorative,
    aria-hidden, never intercepts taps).
- `src/modules/story-stage/StoryStageActivity.ts` — the setup phase
  (steps + summary) mounts the valance above the content; nothing else
  moves.
- `src/styles/child-ui.css` —
  - valance sizing (short strip, pointer-events none);
  - a static golden-glow ended state on the existing
    `story-stage--ended` hook (scene frame warms at endings — no
    animation, reduced-motion safe by construction);
  - story-stage entries in the `prefers-reduced-motion` block: the
    card/choice hover transitions and transforms go still (the module
    has no keyframe animations — this closes the §16/§17 pass).
- **Cue wording pass** (deferred here from slices 5/6): reviewed
  against §19 (concise, scannable, never scripts, no essays) — the
  cues as authored in slices 2/4 already hold; recorded as reviewed
  with no churn.
- `docs/proposals/story-stage-child-entry.md` (new) — the §22
  comparison: Option A (fifth home choice, incl. the featured-wide
  variant) vs Option B (game shelf / More Games), each against the
  required criteria (desktop/mobile layout, one-screen fit, hierarchy,
  navigation cost, discoverability, scalability, complexity), grounded
  in the actual home implementation (2×2 `home-grid`, 600px max,
  cafe-scene backdrop), ending in ONE recommendation for Juan's
  approval. NOT implemented — home stays untouched.
- Tests — `tests/modules/story-stage.test.ts`: valance present in
  setup and absent in the story phase (decorative + aria-hidden);
  existing suites must pass untouched otherwise (art edits are inside
  SVG strings the tests deliberately don't pin).
- `package.json` / `package-lock.json` — `0.3.58`; `MVP_BASELINE.md`.
- Captures — story-room setup (desktop + mobile), polished owl scene
  (lost-log), polished Luna (character shelf or scene), ended glow.

### Explicit Non-Scope / Must Not Change (slice 7)

- **No home-screen change** — the proposal is a document; §22's "do
  not implement both" is honored by implementing NEITHER.
- No new content, families, entities, or cue-wording churn; no runtime
  flow changes; pack/resolver/validator/selection/history modules at 0
  lines; protected games at 0 lines.
- "Do not broaden a slice merely because adjacent work appears
  convenient" — only the named debts and the named environment pieces.

### Assumptions / Blockers

- The valance is the story-room statement for the SETUP screen; story
  scenes keep their framed-stage look (already §16-layered) plus the
  ended glow. A fuller theater treatment is deliberately not attempted
  (§23 guard).
- Blockers: none known.

### Verification Plan

- `npm run typecheck` / `npm test` / `npm run test:viewport` /
  `npm run build` / `git diff --check`.
- Live pass both viewports: setup story-room, owl/Biscuit/Luna scenes,
  ending glow; captures.

## Contract Amendments

(none — the slice shipped exactly as contracted)

## Cold Diff Audit

Gaps first: **none found.** Every contract item is in the diff and
nothing else moved: no home change (the proposal is a document), no
content or flow changes, pack/resolver/validator/selection/history at
0 lines, protected games at 0 lines.

Change-by-change reconstruction (13 files):

1. `src/modules/story-stage/story-art.ts` — owl gains brow feathers,
   warmer shined eyes, wing strokes; Biscuit's peek gains gripping
   paws with toe marks and a small smile; Luna gains a flowing
   two-part mane, an ear, a taller sparkled horn, and a muzzle;
   `storyRoomValanceSvg()` (360×34 curtain strip, six swags + golden
   dots, aria-hidden/focusable=false).
2. `src/modules/story-stage/StoryStageActivity.ts` — the setup steps
   and the summary mount the valance (decorative span, aria-hidden);
   no flow change.
3. `src/styles/child-ui.css` — valance sizing (94vw/560px cap,
   `pointer-events: none`, `line-height: 0`); static golden ended
   glow on the existing `story-stage--ended` hook; story-stage
   card/choice transition + hover-transform stilled inside the global
   `prefers-reduced-motion` block (verified placed inside the media
   query).
4. `tests/modules/story-stage.test.ts` — valance present + aria-hidden
   in setup, absent in the story phase.
5. `docs/proposals/story-stage-child-entry.md` (new) — the §22
   comparison grounded in the actual 2×2/600px home: Option A1
   (five equal tiles) rejected on hierarchy/fit, Option A2 (featured
   wide story card) vs Option B (game shelf) compared on every
   required criterion; recommendation **A2 now, shelf at 6+ games**;
   explicitly NOT implemented, decision left to Juan.
6. `docs/work-contracts/v0.3.58-story-stage-slice7.md` — this file.
7. `MVP_BASELINE.md` — v0.3.58 line + arc-complete slice-7 sentence.
8.–11. captures — story-room setup (desktop + mobile), polished
   owl/Biscuit log scene, ending glow.
12./13. `package.json` / `package-lock.json` — 0.3.58.

Cue wording pass: reviewed against §19 — concise beats, keep-true
facts, one ask, optional silly, no scripts; the slices-2/4 authoring
already holds, so no churn (recorded as reviewed-final).

Reduced-motion pass: the module has no keyframe animations; its only
motion (card/choice hover transitions) is now stilled under
`prefers-reduced-motion`; the ended glow is static by construction.

Verification actually run: `npm run typecheck` (clean); `npm test`
(57 files / 762 tests); `npm run test:viewport` (6/6); `npm run build`
(clean); `git diff --check` (clean). Live pass at 1366×900 + 390×844:
story-room setup with valance + mode badge, owl/Biscuit polish in the
lost-log scene, Luna on the shelf, golden ending glow — 4 new captures.

Gap audit: **DONE**.
