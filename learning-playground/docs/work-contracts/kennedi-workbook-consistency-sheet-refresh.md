# Kennedi Workbook: Refresh the 8-Pose Kennedi Consistency Sheet

Follow-up to PR #139, which replaced locked pose `04-helping`. That PR left
`docs/art/kennedi-consistency-sheet.png` showing the old pose, and said so in
`design-system.md`.

## Before Code

### Root Cause

`workbook/docs/art/kennedi-consistency-sheet.png` is the reference image of
the eight locked Boss Kennedi poses. It still shows the pre-#139
`04-helping`. Nothing in the repo records how the sheet was made, so it
could not simply be re-run when a pose changed.

The recipe was recovered by reproduction: ImageMagick `montage` of the eight
locked PNGs as of commit e1f2fce (where the sheet was created), with
`-tile 4x2 -geometry 400x400+15+15 -border 1 -bordercolor gray
-background white -font DejaVu-Sans-Bold -pointsize 20` and the labels
printed on the sheet, reproduces the committed sheet with 0 differing pixels
(`compare -metric AE`).

### Correct Fix Must Touch

- `workbook/tools/make-kennedi-consistency-sheet.sh` -- new; the recovered
  montage command, reading the current `locked-poses/0[1-8]-*.png`, so the
  sheet can be rebuilt whenever a locked pose changes.
- `workbook/docs/art/kennedi-consistency-sheet.png` -- regenerated with that
  script from the current locked poses.
- `workbook/docs/design-system.md` -- drop the "predates the 2026-09-23
  04-helping replacement" note on the sheet reference; point to the script.
- `workbook/docs/art/asset-provenance.md` -- record that the sheet is built
  by the script and was regenerated after the 04 replacement.
- `learning-playground/docs/work-contracts/` -- this contract.

### Must Not Change

- Every locked pose PNG/SVG, the simplified tier, all animal assets.
- `docs/art/puppy-consistency-sheet.png` and `docs/art/concept-contact-sheet.png`.
- The sheet's layout, tile order, labels, font and colors -- only tile 4's
  image content may differ.
- `workbook/src/**`, `scripts/**`, other `tools/**`, `dist/**`,
  `dist-proof/**`, `dist-book-2/**`, CI workflows.
- The "Known exceptions" text in `design-system.md` (03/08 collars, 04
  deviations) -- still accurate.
- Everything under `learning-playground/` except this contract file.

## Contract Amendments

None yet.

## Cold Diff Audit

### Gaps

- change without contract trace: none. `git diff --stat origin/main..HEAD`
  lists 5 files, all named in "Correct Fix Must Touch".
- contract requirement not delivered: none.
- protected surface touched: none. No locked pose, animal asset, other
  sheet, `src/`, `scripts/`, other `tools/`, `dist*/` or CI file changed;
  `npm run all` leaves `dist/` byte-identical to main (PDF bytes excepted,
  as CI excludes them).

### Change By Change Reconstruction

1. `workbook/tools/make-kennedi-consistency-sheet.sh` (new, mode 100755) --
   `:25-37` the recovered `montage` command over
   `locked-poses/0[1-8]-*.png` with the sheet's labels; `:36` adds
   `-define png:exclude-chunk=date,time` (no pixel effect; makes reruns
   byte-identical); `:22` writes to an `mktemp` file in the output
   directory, `:39-45` copies the replaced file's mode onto it (or
   0666 & ~umask when there is none), `:46` renames it into place; `:17`
   `set -euo pipefail` and the EXIT trap remove the temp file on failure.
2. `workbook/docs/art/kennedi-consistency-sheet.png` -- regenerated by (1).
   Same format as before (1728x920, 16-bit RGBA, bKGD + cHRM chunks). Every
   changed pixel is inside tile 4's image area (x 1312-1711, y 16-415);
   frames, labels and tiles 1-3 and 5-8 are pixel-identical.
3. `workbook/docs/design-system.md:282-283` -- the "predates the 2026-09-23
   04-helping replacement" note replaced by a pointer to the script.
4. `workbook/docs/art/asset-provenance.md:88-93` -- the sheet entry now
   names the script, its reproduction result, and the 2026-09-23 refresh.
5. This contract file.

### Contract Traceability

| File | Contract item |
|---|---|
| `tools/make-kennedi-consistency-sheet.sh` | Must Touch 1 |
| `docs/art/kennedi-consistency-sheet.png` | Must Touch 2 |
| `docs/design-system.md` | Must Touch 3 |
| `docs/art/asset-provenance.md` | Must Touch 4 |
| this file | Must Touch 5 |

### Verification

Run locally in an isolated worktree off `origin/main` (6eab20e):

- Recipe recovery: the montage command with DejaVu-Sans-Bold at pointsize
  20, run over the e1f2fce locked poses, reproduces the committed original
  sheet with `compare -metric AE` = 0. (Liberation Sans Bold 20: 12327 px
  differ; every other font/size tried produced the wrong canvas size.)
- Script tests, run in a scratch copy of the workbook layout: (a) e1f2fce
  poses -> AE 0 against the original sheet; (b) two consecutive runs ->
  byte-identical output (`cmp`); (c) one pose file removed -> exit 1, the
  existing sheet's sha256 unchanged, no temp files left behind; (d) default
  output path, existing file mode 644 preserved.
- Real run: changed-pixel bbox confined to tile 4's image area (see
  reconstruction item 2); sheet inspected visually.
- `workbook`: `npm ci`, `npm run build && npm run verify` -> VERIFY PASSED;
  `npm run all` -> VERIFY PASSED; `git status -- dist/` clean after
  restoring the timestamp-only PDF.
- Not run: `learning-playground` tests (this PR's only change there is this
  Markdown file, which no test, script or config reads -- confirmed for the
  previous identical-shape PR, where the full suite passed); GitHub Actions
  runs on push.
