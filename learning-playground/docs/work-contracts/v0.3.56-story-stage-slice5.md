# v0.3.56 Kennedi's Story Stage — slice 5: Tell It Together

Mode B (spec §5): the adult improvises over the authored structure. A
parent setting (`narrated` | `together`), the Together runtime (adult
cue panel instead of automatic narration, authored-narration fallback),
and mode persistence tests. Spec §5/§6/§19 + §25/slice-5.

## Before Code

### Root Cause (slice)

The cues have lived in the content since slice 2 — every scene carries
beat / keepTrue / ask / silly — but no surface consumes them, and the
runtime can only narrate. §5's second first-class mode needs: a
parent-owned mode setting (child never chooses, §6), a Together runtime
that presents the cue and stays quiet, and the §5 guarantee that the
session is never stranded (an obvious control plays the authored
narration when the storyteller gets stuck).

### Required Change Surface

- `src/types/storage.ts` — `ParentSettings.story_mode: 'narrated' |
  'together'`; `src/core/storage.ts` — `story_mode: 'narrated'` in
  `DEFAULT_SETTINGS` (the existing defaults-merge in `getSettings`
  gives stored-settings backward compatibility for free).
- `src/modules/parent-panel/ParentPanel.ts` —
  - `createStoryModeSection` (gate-phrase/voice section pattern):
    title, one-line explanation, two mode buttons with the active one
    marked, `saveSettings({...settings, story_mode})` + panel
    re-render. Living in the parent panel = parent-gated by location;
    no per-scene gating anywhere (§6).
  - Story Stage launch card refreshed (it still describes slice 1):
    status shows the ACTIVE mode + "Pick Three story builder"; Entry
    metric "Pick Three setup"; new Mode metric. This is §6's "active
    mode visible to the adult before the story starts" at the launch
    surface.
- `src/app/main.ts` — passes
  `storyMode: settings.story_mode === 'together' ? 'together' : 'narrated'`
  (anything unexpected degrades to narrated) into `renderStoryStage`.
- `src/modules/story-stage/StoryStageActivity.ts` —
  - Options gain optional `storyMode` (default `'narrated'` — narrated
    tests and behavior unchanged).
  - Setup screen gains a small adult-facing mode badge ("Tell Me a
    Story" / "Tell It Together") — §6 visibility before the story
    starts, at the story surface itself.
  - **Together story phase**: scenes do NOT auto-narrate (opening and
    scene speech suppressed; speech.stop() still fires on transitions
    so a fallback playback never overlaps); the caption starts empty;
    an adult cue panel renders between scene and child controls —
    header "Storyteller cue" + collapse/reveal toggle, cue lines (beat,
    keep true, ask, optional silly twist), and a **"Play the story
    line"** fallback that speaks the authored narration AND reveals it
    in the caption (never stranded, §5). Collapse state persists across
    scenes within the story. The panel is visually adult (small, muted,
    parchment) and never carries a story decision (§19); child
    choices/Continue are untouched. Tell It Again in together mode
    restarts silently.
  - Setup selection speech (step asks + spoken labels) stays in BOTH
    modes — §7's spoken labels are a child selection aid, not story
    narration.
- `src/styles/child-ui.css` — mode badge + cue panel styles (readable
  without covering the scene, compact at 390px, no scrolling between
  cue and choices; conditional rendering — no `hidden` attributes).
- Tests —
  - `tests/contract/story-mode.test.ts` (new): default `narrated`;
    round-trip persistence of `together` through `StorageService`;
    stored-settings-without-the-key backward compatibility; runtime
    coercion of unexpected values to narrated.
  - `tests/modules/story-stage.test.ts`: together runtime — no
    automatic narration speech after Start (setup speech only); cue
    panel shows the resolved beat/keepTrue/ask lines; fallback speaks
    + reveals the authored narration; collapse hides and reveal
    restores the cue lines; a full choice traversal reaches an ending
    with zero narration speaks; Tell It Again restarts silently; the
    setup badge reflects the mode in both modes; narrated behavior
    pinned unchanged (existing tests keep passing untouched).
- `package.json` / `package-lock.json` — `0.3.55`; `MVP_BASELINE.md`.
- Captures — together-mode scene with cue panel (open + collapsed) and
  the Parent Panel mode section, desktop 1366×900 + mobile 390×844.

### Explicit Non-Scope / Must Not Change (slice 5)

- Narrated mode: byte-identical behavior (default unchanged; no new
  UI in narrated child flow beyond the setup badge).
- No per-session override UI this slice (the §6 "if it fits cleanly"
  clause — the Parent Panel toggle is one tap behind the gate; a
  second override surface adds ambiguity, recorded as a deliberate
  no).
- Cue WORDING stays as authored in slices 2/4 (polish was always
  slice-5-UI-exists-first; a wording pass is now unblocked but belongs
  to slice 7 alongside the art polish, keeping this slice mechanical).
- No story history (slice 6), no home change (slice 7), no event sink,
  pack/resolver/validator/selection modules at 0 lines, protected
  games at 0 lines.

### Assumptions / Blockers

- The child may see the cue panel text (shared screen) — it contains
  only safe, authored, non-interactive adult guidance; the §19
  requirement is that it never becomes a story-decision target, which
  the layout enforces (separate panel, no choice semantics).
- Blockers: none known.

### Verification Plan

- `npm run typecheck` / `npm test` / `npm run test:viewport` /
  `npm run build` / `git diff --check`.
- Live dev-server pass: switch mode in Parent Panel → launch → together
  play-through using cues + one fallback playback + collapse, both
  viewports; switch back to narrated → unchanged narrated flow.

## Contract Amendments

- **Version renumbered 0.3.55 → 0.3.56.** Codex's PR #84 (Bear video
  human narration) merged as v0.3.55 mid-slice; origin/main was merged
  into this branch and the slice renumbered.
- **`.story-stage__caption:empty { display: none; }`** added after the
  live pass: in together mode the intentionally-empty caption rendered
  as a stray padded pill under the scene. Conditional-content styling,
  not a `[hidden]` attribute.

## Cold Diff Audit

Gaps first: **none found.** Every contract item is in the diff; two
amendments recorded above. Non-scope held: narrated child flow
unchanged except the setup mode badge (named in-contract); no
per-session override (deliberate no, recorded); cue wording untouched;
pack/resolver/validator/selection modules at 0 lines; protected games
at 0 lines; no event sink.

Change-by-change reconstruction (slice files; the merge from
origin/main additionally brings PR #84's video-vault files, unrelated
and untouched by this slice):

1. `src/types/storage.ts` — `ParentSettings.story_mode:
   'narrated' | 'together'` (documented).
2. `src/core/storage.ts` — `story_mode: 'narrated'` in
   `DEFAULT_SETTINGS`; the existing `{...DEFAULT_SETTINGS, ...parsed}`
   merge makes legacy stored settings default to narrated (test-pinned).
3. `src/app/main.ts` — story-stage route passes
   `storyMode: settings.story_mode === 'together' ? 'together' : 'narrated'`
   (unexpected values degrade to narrated).
4. `src/modules/story-stage/StoryStageActivity.ts` — `StoryMode` type +
   optional `storyMode` option (default narrated); `MODE_LABELS`; setup
   mode badge; together story phase: `speakScene` stops-only,
   launch/Tell-it-again narration gated to narrated, caption empty in
   together, `renderCue` (header + Hide/Show toggle with
   session-persistent collapse, beat/keep-true/ask/silly lines, "Play
   the story line" fallback that speaks AND reveals the authored line).
5. `src/modules/parent-panel/ParentPanel.ts` —
   `createStoryModeSection` (explanation + two aria-pressed option
   buttons, saveSettings + re-render — the gate-phrase section
   pattern); `createParentGameLaunchSection` now takes settings; Story
   Stage card refreshed (mode in status, Entry "Pick Three setup", Mode
   metric).
6. `src/styles/child-ui.css` — mode badge, cue panel (dashed parchment,
   small type, uppercase header, toggle, fallback), `caption:empty`
   pin.
7. `src/styles/parent-ui.css` — story-mode section styles (explanation,
   option buttons, active state).
8. `tests/contract/story-mode.test.ts` (new, 4 tests) — default,
   round-trip persistence, legacy fallback, switch-back.
9. `tests/modules/story-stage.test.ts` (+7 tests) — badge in both
   modes; silent launch with resolved cue lines; zero-speech full
   traversal + silent Tell It Again; fallback speaks + reveals;
   collapse persists across scenes and reveals the next scene's beat;
   narrated renders no cue panel.
10. `tests/contract/parent-game-launch.test.ts` /
    `parent-panel-summary.test.ts` — fixtures gain `story_mode`
    (type-required).
11. `docs/work-contracts/v0.3.56-story-stage-slice5.md` — this file.
12. `MVP_BASELINE.md` — v0.3.56 version line + slice-5 sentence (on top
    of the merged v0.3.55 baseline).
13.–16. `docs/captures/story-stage/` — together-cue-panel,
    together-fallback-reveal, together-cue-collapsed,
    together-cue-panel-mobile, parent-story-mode-section (desktop +
    390×844).
17./18. `package.json` / `package-lock.json` — 0.3.56 (kept ours over
    the 0.3.55 merge conflict).

Verification actually run: `npm run typecheck` (clean); `npm test`
(56 files / 748 tests — includes the merged video-vault suite);
`npm run test:viewport` (6/6); `npm run build` (clean);
`git diff --check` (clean). Live dev-server pass: mode switched to
together IN the Parent Panel section (toggle round-trip verified:
storage value, active button, launch-card status all updated), full
together play-through at 1366×900 (badge → silent scenes → cue panel →
fallback reveal → collapse) and the cue panel at 390×844 (scene
preserved, no scrolling between cue and Continue), zero console
errors; mode reset to narrated afterwards.

Gap audit: **DONE**.
