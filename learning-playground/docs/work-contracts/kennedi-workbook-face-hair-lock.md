# Kennedi Workbook: Lock Kennedi's Face and Hair Across the 8 Locked Poses

Owner review (2026-09-24) of the consistency sheet: "the faces and some
features are different among the different pictures." Supersedes PR #140
(sheet refresh only), whose build script is folded in here.

## Before Code

### Root Cause

The eight locked Boss Kennedi poses
(`workbook/design-source/boss-kennedi/locked-poses/0[1-8]-*.png`) show eight
similar girls, not one. At matched face width the eyes vary (small dots,
large glossy, heavy-lashed), the nose (none, curve, prominent "c",
freckled), bangs (curtain, swept, center part, spiky), pigtails (curly,
long straight, short high puffs), and apparent age.

Cause: the pipeline asks one knob, FLUX.1 Redux strength, to carry both
pose change and identity. `asset-provenance.md` records that strength >=0.2
snaps poses back to the standing reference, so posture changes run at
~0.08 -- where Redux transfers style and outfit but almost none of the
face, so FLUX invents a new face each time. The #139 audit that called 6 of
7 poses consistent scored trait *presence* (has pigtails, has eyelashes),
which this failure passes; it did not compare faces.

Fix, prototyped and owner-approved (scratch, 2026-09-24): separate the two
jobs. Keep every pose as drawn and repaint only the head (face, hair,
pigtails) against one canonical head, by FLUX.1 Fill "in-context"
(canonical on the left half of one canvas, target on the right, target
head masked), composited back through the feathered head mask. Canonical
(owner pick): pose 07 with its face repainted by Fill + Redux 0.6 (seed 2)
from the 01 head. Head-only Redux at that strength does not disturb the
pose, because the pose is not regenerated.

### Correct Fix Must Touch

- `workbook/design-source/boss-kennedi/locked-poses/0[1-8]-*.png` -- the
  eight owner-approved head-locked rasters.
- `.../locked-poses/0[1-8]-*.svg` -- re-traced with
  `tools/vectorize-line-art.sh` (default threshold).
- `.../simplified-tier/0{2,4,5}-*-simplified-source.png` and
  `-simplified.svg` -- regenerated from the new locked PNGs with the
  documented looser potrace parameters.
- `workbook/tools/make-kennedi-consistency-sheet.sh` -- new (from PR #140,
  unchanged): the recovered montage recipe.
- `workbook/docs/art/kennedi-consistency-sheet.png` -- regenerated with it.
- `workbook/src/content/pages/page-03-meet-boss-kennedi.mjs` (06/08/03 card
  viewBoxes + bbox comment) and `page-06-helping-mission.mjs` (04 card
  viewBox + bbox comment) -- re-measured with the documented `getBBox()`
  technique wherever the new art's bbox differs. Nothing else in those files.
- `workbook/docs/art/asset-provenance.md` -- record the face/hair lock
  (canonical, method, per-pose seeds, verification) and correct the #139
  "held identity in 6 of 7" claim.
- `workbook/docs/design-system.md` -- add the canonical face/hair to the
  locked identity traits and the head-lock step to the mechanism; point to
  the sheet script.
- `workbook/dist/**` -- regenerated by `npm run all` and committed.
- `learning-playground/docs/work-contracts/` -- this contract.

### Must Not Change

- Anything outside each pose's head: body, pose, clothes, badge, puppy,
  props. Verified per pose: 0 changed pixels outside the head mask plus
  feather margin (for 04 also the strip above its head cleared of the old
  hair puffs).
- The known outfit exceptions (03/08 crew necks; 04 shorts + rectangular
  badge) -- not addressed here; still documented.
- Concepts, animal/puppy assets, objects, scenes, `puppy-consistency-sheet.png`,
  `concept-contact-sheet.png`.
- Page layouts, text, labels and `meta.correctAnswers`; components,
  styles, `scripts/**`, other `tools/**`, `verify.mjs`, CI workflows.
- `workbook/dist-proof/**`, `src/content/proof-pages/**` (historical
  record, not rebuilt), `workbook/dist-book-2/**` (uses no Kennedi pose).
- Everything under `learning-playground/` except this contract file.

## Contract Amendments

None yet.

## Cold Diff Audit

### Gaps

- change without contract trace:
- contract requirement not delivered:
- protected surface touched:

Do not declare done while any gap stands.

### Change By Change Reconstruction

### Contract Traceability

### Verification
