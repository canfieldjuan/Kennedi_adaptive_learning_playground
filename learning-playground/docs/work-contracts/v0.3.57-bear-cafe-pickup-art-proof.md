# v0.3.57 Bear Cafe Pickup-Window Production-Art Proof

One-scene visual proof under `art-production-assets.contract.md`. This slice
does not roll the style across Bear Cafe and does not claim owner look approval.
Package version assignment is deferred until an approved proof is integrated;
the proof branch does not consume a release number merely for review. Current
main is v0.3.58 after integration.

## Before Code

### Root Cause

The Bear Cafe delivery stage is playable and illustrated, but its visual
composition remains a sparse arrangement of a small dynamic tray and a small
customer window over the generic cafe counter. It does not yet prove the richer
production-art workflow, editable source, environmental density, character
scale, responsive crop, or ownership record required before a broad graphics
rollout.

The cause is not missing gameplay. The stage has no production-editor source or
dedicated local environment export; its scene framing is assembled from generic
background SVG plus CSS around the dynamic customer and order.

### Correct Fix Must Touch

- `design-source/bear-cafe/bear-cafe-pickup-window-proof.svg` (new): editable
  Inkscape source composed from original basic shapes and paths only.
- `public/assets/images/bear-cafe-pickup-window-proof.svg` (new): optimized local
  SVG export with a stable `viewBox`, no fonts, remote references, stock, library
  content, or inaccessible instructions.
- `src/modules/kennedis-orders/KennedisOrdersActivity.ts`: add one decorative,
  non-interactive local scene layer behind the existing dynamic order and bear.
- `src/styles/child-ui.css`: compose the proof asset, dynamic order, window, and
  delivery control at desktop and mobile sizes without reducing tap targets.
- `tests/modules/cafe-environment.test.ts`: prove the local decorative asset is
  present only in delivery, cannot intercept input, and preserves the existing
  single delivery completion path.
- `docs/art/asset-provenance.md`: add the complete Category A proof-family entry,
  with owner look approval explicitly pending.
- `docs/captures/bear-cafe/`: add proposed desktop/mobile screenshots and one
  existing-versus-proposed contact sheet. Reuse the existing delivery capture as
  the before image rather than duplicating it.
- This work contract: record art direction, tool/export evidence, verification,
  and the final line-cited cold diff audit.

### Must Not Change

- No caller, order, food, decoration, bear, tray, bell, handoff, completion, or
  home-screen art replacement. This proof is the pickup-window environment only.
- No correct answer, order evaluation, event timing, delivery commit, timer,
  evidence, mastery, transfer, curriculum, difficulty, route, parent behavior,
  persistence, speech, sound, or completion-semantic change.
- No child-facing text in the asset, no new focus target, no pointer interception,
  no smaller Deliver control, and no animation or reduced-motion behavior change.
- No Canva/Figma library content, stock media, third-party font/icon/texture,
  generative image tool, external URL, runtime design-platform dependency, or
  account/network requirement.
- No other Bear Cafe stage or game, no broad CSS refactor, dependency change,
  Content Foundry use, Story Stage lane, or unrelated cleanup.
- Do not edit the unrelated `.github/workflows/pr-review-wake.yml` in the shared
  checkout or another session's branch, worktree, PR, or persisted state.

### Art Direction Standard

- Preserve the established purple ink and warm flat-fill language while adding
  a clear cafe pickup identity: large framed service opening, scalloped awning,
  tile/wood structure, shelf and kitchen silhouettes, and intentional empty
  zones for the live bear and tray.
- The active order, live character, and Deliver control remain the strongest
  visual elements. Background contrast stays lower and no decorative object
  resembles a selectable food or count target.
- Desktop and phone crops retain the pickup opening, character, order, and
  action. Minor edge props may crop; interactive objects may not shrink for art.

### Source and Ownership Standard

- Tool: Inkscape, a local vector-design application; record the exact installed
  version in provenance before export.
- Origin: Category A original project artwork made from basic shapes and
  paths; no imported third-party or project legacy art is embedded in the source.
- AI assistance: none.
- Editable source: committed Inkscape SVG; production export: separately
  optimized standalone local SVG without unnecessary editor metadata.
- Status at PR open: `rights_verified`, with owner look approval pending. Do not
  mark `look_approved` or `production_ready` before the owner approves the proof.

### Verification Plan

- Inspect the source in Inkscape and independently render the production SVG.
- Validate SVG XML, `viewBox`, local-only references, absence of text/fonts,
  reasonable path/object count, and transparent/dynamic-content zones.
- Run focused Bear Cafe environment/runtime tests, full `npm test`, typecheck,
  build, viewport tests, lint if present, work-contract check, and diff check.
- Browser-smoke the success path through delivery at desktop and 390x844;
  verify visual crop, non-overlap, console state, single completion event, and
  unchanged event metadata.
- Generate proposed desktop/mobile screenshots and a before/proposed contact
  sheet, then stop for owner look approval before any broader Bear Cafe art.

## Contract Amendments

### Inkscape Authoring Tool

The operator chose to add Inkscape after the initial contract named the already
installed LibreOffice Draw fallback. No art had been created. The source format
and tool references are changed before production work begins; every ownership,
export, runtime, and gameplay boundary remains unchanged.

### Current-Main Integration

Main advanced through Story Stage slice 7 / v0.3.58 while the proof was being
prepared. Integrate that exact base, retain its visual CSS and child-entry
proposal unchanged, and rerun every gate against the combined stylesheet. The
merge resolved automatically; no Story Stage file appears in the proof diff.

## Cold Diff Audit

### Gaps

No standing implementation or contract gap.

- Change without contract trace: none.
- Contract requirement not delivered: none.
- Protected surface touched: none.
- Owner look approval remains pending by design. The ledger status is
  `rights_verified`, not `look_approved` or `production_ready`; broad Bear Cafe
  rollout and merge remain blocked until the owner evaluates this proof.
- The first fake-DOM assertions attempted to query a nested image node and then
  treated SVG namespace declarations as runtime links. Both were rejected and
  replaced with assertions that match the harness boundary: local image markup,
  decorative attributes, and unchanged completion behavior. Real image loading,
  pointer behavior, and geometry were verified in Chromium.

### Change By Change Reconstruction

1. `design-source/bear-cafe/bear-cafe-pickup-window-proof.svg:1-80` is the
   editable Inkscape source: 1200x640, seven named editable layers, original
   basic shapes/paths, and reserved live zones for the runtime order and bear.
2. `public/assets/images/bear-cafe-pickup-window-proof.svg:1-24` begins the
   separately exported plain SVG with the same 1200x640 `viewBox`; the full file
   contains no text, font, link, remote reference, or embedded data.
3. `src/modules/kennedis-orders/KennedisOrdersActivity.ts:759-778` adds one local
   empty-alt, `aria-hidden`, non-draggable image behind the existing dynamic
   order and bear. Lines 781-789 retain the existing Deliver button and handler.
4. `src/styles/child-ui.css:3059-3104` sizes the static scene, disables pointer
   interception, and positions the live order and customer over reserved art
   zones. Lines 4064-4079 preserve phone scale; lines 4478-4497 preserve the
   established short-landscape delivery grid and action size.
5. `tests/modules/cafe-environment.test.ts:78-107` pins the approved local asset
   markup and decorative attributes while retaining the existing exact-one
   `order_delivered` completion assertion and handoff transition.
6. `docs/art/asset-provenance.md:70-99` records Category A origin, Inkscape
   version, editable/production paths, complete rights answers, no third-party
   or AI use, capture paths, and pending owner approval.
7. `docs/captures/bear-cafe/pickup-window-proof-desktop.webp`,
   `pickup-window-proof-mobile.webp`, and
   `pickup-window-proof-contact-sheet.webp` provide the final integrated
   desktop/mobile evidence and existing-versus-proposed comparison.
8. This work contract records the bounded art direction, tool amendment,
   current-main integration, verification, and cold reconstruction.

### Contract Traceability

- Editable source, production export, provenance, captures, and this contract
  trace directly to Correct Fix Must Touch and the production-art delivery gate.
- The runtime image, responsive CSS, and focused test are the minimum declared
  integration hook needed to display and verify the proof.
- No caller/order content, dynamic character/food/tray art, other stage/game,
  event/evidence code, curriculum, route, parent, storage, dependency, package,
  or Story Stage file appears in the final diff, satisfying Must Not Change.
- The source/export change from LibreOffice Draw fallback to Inkscape traces to
  the pre-art Inkscape Authoring Tool amendment.

### Verification

- Inkscape: PASS - 1.4.4 Flatpak; editable source has seven named layers.
- SVG validation: PASS - both files parse, report `viewBox="0 0 1200 640"`,
  contain no text/href/data/remote attribute, and independently render. Source:
  71 XML elements / 10,387 bytes. Production: 72 elements / 12,657 bytes.
- Focused Bear Cafe tests: PASS - 2 files, 37 tests.
- `npm test`: PASS - 46 Content Foundry Python tests (1 live/GPU test skipped as
  designed), plus 57 Vitest files and 762 Vitest tests.
- `npm run typecheck`: PASS.
- `npm run build`: PASS - 125 modules transformed.
- `npm run test:viewport`: PASS - 6 Playwright tests, including Bear Cafe
  delivery reachability at 568x320.
- `npm run lint --if-present`: PASS - no lint script is configured.
- `node scripts/check-work-contract.mjs`: PASS.
- `git diff --check`: PASS.
- Desktop Chromium (1280x720): PASS - local art loaded at 1200x640; scene
  760x405, bear 190x190, Deliver 132x66, no page overflow or Vite overlay.
- Mobile Chromium (390x844): PASS - no page overflow; scene 374x200, bear 94x94,
  order 82x57, Deliver 132x66, and all required content remains on screen.
- Browser event boundary: PASS - one Deliver tap emitted exactly one existing
  `completed` event with `event_name: order_delivered`, unchanged vocabulary and
  transfer metadata, and transitioned to `handoff`.
- Final proposed desktop/mobile captures and the before/proposed contact sheet
  were regenerated from the integrated v0.3.58 tree.

### Gap Audit

DONE for the one-scene proof. Owner look approval is the explicit stop before
merge or broader rollout.
