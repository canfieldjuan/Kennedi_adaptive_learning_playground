# v0.3.2 Child Home Bear Cafe Entry Contract

## Before Code

### Root Cause

The current child home grid still uses the second slot for `Videos`, while the product decision for this slice is to make that slot start the full Bear Cafe shift. The Bear Cafe runtime and activity chain already exist; the missing piece is the child-facing home entry route to `#activity/kennedis-orders-banana-001`.

### Correct Fix Must Touch

- `src/modules/home/HomeScreen.ts` to replace the second home tile with a Bear Cafe entry that starts the full shift at `kennedis-orders-banana-001`.
- `tests/contract/parent-game-launch.test.ts` and/or a focused child home contract test to update the old no-home-entry expectation and prove the new four-slot home grid routes to Bear Cafe.
- `docs/work-contracts/v0.3.2-child-home-bear-cafe-entry.md` to record the before-code contract and final audit.
- `CURRENT_STATUS_AND_ROADMAP.md` and `MVP_BASELINE.md` to update the current product state.
- `package.json` and `package-lock.json` to bump the app version if verification passes.

### Must Not Change

- Do not start directly on Daddy Bear; the home entry must start the full shift at `kennedis-orders-banana-001`.
- Do not change the Bear Cafe runtime, activity JSON, activity chain, event schema, activity schema, mastery engine, transfer coverage engine, recommendation engine, storage, export/reset behavior, parent approval semantics, or parent panel launch behavior.
- Do not add new game modules, new activities, `Nature Camera Safari`, backend services, auth, cloud sync, AI tutor chat, open web content, rewards, streaks, leaderboards, public sharing, or automatic adaptive routing.
- Do not add a fifth home tile; the home grid must remain exactly four child-facing choices.
- Do not weaken safety, parent approval, transfer truth, or mastery rules.

## Contract Amendments

- Vercel preview failed after PR creation because the Git-connected project still had `rootDirectory: null`, so Vercel attempted to build the repository root instead of `learning-playground`. The Vercel project setting was updated externally to `rootDirectory: "learning-playground"` to match the repo layout and existing Vite build/output settings. This did not change the app behavior scope.

## Cold Diff Audit

### Gaps

- change without contract trace: none
- contract requirement not delivered: none
- protected surface touched: none

### Change By Change Reconstruction

- `src/modules/home/HomeScreen.ts:29` through `src/modules/home/HomeScreen.ts:35` replaces the former Videos tile with a Cafe tile in the same second home-grid slot and routes it to `#activity/kennedis-orders-banana-001`.
- `tests/contract/child-home-cafe-entry.test.ts:27` through `tests/contract/child-home-cafe-entry.test.ts:50` renders the home screen, proves the four child choices are Words/Cafe/Math/Art, proves Videos is absent, clicks the Cafe button, and verifies speech plus the full-shift route.
- `tests/contract/parent-game-launch.test.ts:40` through `tests/contract/parent-game-launch.test.ts:58` updates the previous no-home-entry expectation to the new Cafe-replaces-Videos contract.
- `tests/contract/parent-game-launch.test.ts:61` through `tests/contract/parent-game-launch.test.ts:83` keeps the no-new-game-module and no-Nature-Camera guard.
- `MVP_BASELINE.md:3` through `MVP_BASELINE.md:19` updates the baseline to v0.3.2, documents Words/Cafe/Math/Art, and keeps Video Vault as direct-route reachable.
- `CURRENT_STATUS_AND_ROADMAP.md:7` through `CURRENT_STATUS_AND_ROADMAP.md:280` updates current status, protected scope, next-phase notes, and product-shape wording for the child home Bear Cafe entry.
- `package.json:4` and `package-lock.json:3` through `package-lock.json:9` bump the app version to `0.3.2`.
- `docs/work-contracts/v0.3.2-child-home-bear-cafe-entry.md:1` through `docs/work-contracts/v0.3.2-child-home-bear-cafe-entry.md:64` records this contract and audit.

### Contract Traceability

- Required home-grid replacement: delivered by `src/modules/home/HomeScreen.ts:29` through `src/modules/home/HomeScreen.ts:35`.
- Required full-shift start, not direct Daddy Bear start: delivered by the route to `kennedis-orders-banana-001` in `src/modules/home/HomeScreen.ts:35` and tested in `tests/contract/child-home-cafe-entry.test.ts:49` through `tests/contract/child-home-cafe-entry.test.ts:50`.
- Required tests for the new four-slot home grid and Cafe route: delivered by `tests/contract/child-home-cafe-entry.test.ts:27` through `tests/contract/child-home-cafe-entry.test.ts:50` and `tests/contract/parent-game-launch.test.ts:40` through `tests/contract/parent-game-launch.test.ts:58`.
- Required docs/version alignment: delivered by `MVP_BASELINE.md`, `CURRENT_STATUS_AND_ROADMAP.md`, `package.json`, and `package-lock.json`.
- Required protected surfaces left alone: delivered; Bear Cafe runtime, activity JSON, activity chain, schemas, mastery, transfer coverage, recommendation, storage, export/reset, parent approval, and parent panel launch behavior were not changed.

### Verification

- `git diff --check`: passed.
- `npm test`: passed, including the work-contract check and 31 Vitest files / 143 tests.
- `npm run typecheck`: passed.
- `npm run build`: passed.
- `vercel build --cwd learning-playground`: passed.
- Vercel project setting check: `rootDirectory` is `learning-playground`, `buildCommand` is `npm run build`, and `outputDirectory` is `dist`.
- Browser verification at `http://127.0.0.1:5178/`: home rendered Words, Cafe, Math, and Art; no Vite overlay; clicking Cafe routed to `#activity/kennedis-orders-banana-001`; completing and delivering the first banana order exposed `Next order`; clicking `Next order` routed to `#activity/kennedis-orders-two-cookies-001` and showed Daddy Bear.
